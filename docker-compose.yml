services:
  goalgetter:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: goalgetter
    environment:
      ASPNETCORE_ENVIRONMENT: Production
      ASPNETCORE_URLS: http://+:8080
    ports:
      - "8080:8080"
    restart: unless-stopped